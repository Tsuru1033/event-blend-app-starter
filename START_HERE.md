# ここから始めてください

このスターターキットは、イベント参加者がQRコードから回答し、その結果を集計・発表するためのWebアプリです。

プログラミング、GitHub、Supabase、Vercelを使ったことがない方でも、AIへ画面のスクリーンショットを送りながら、一つずつ設定を進められるように作られています。

---

## このアプリで使用するサービス

### GitHub

アプリのプログラム、設定ファイル、説明書を保管する場所です。

### Supabase

参加者の回答、今回の参加人数、集計者のログイン情報を保存する場所です。

### Vercel

GitHubに保存したアプリをインターネット上へ公開するサービスです。

各サービスは、アプリを利用する方自身のアカウントで作成してください。提供元のアカウントを共同利用する必要はありません。

---

## AIと一緒に作業してください

このスターターキットは、AIと相談しながら導入・変更することを前提にしています。

最初に、GitHubのページに保存されている次のファイルをAIへ確認してもらってください。

- `START_HERE.md`
- `README.md`
- `AGENTS.md`
- `AI_HANDOFF.md`
- `AI_PROMPTS.md`
- `docs/00_最初にお読みください.md`

### AIへ最初に送る文章

次の文章をコピーして、使用するAIへ貼り付けてください。

> 私は、AI、GitHub、Vercel、Supabase、プログラミングの初心者です。
>
> このアプリを導入・編集したいので、最初にSTART_HERE.md、README.md、AGENTS.md、AI_HANDOFF.md、AI_PROMPTS.md、docs/00_最初にお読みください.mdを確認してください。
>
> 一度に複数の操作を案内せず、画面のスクリーンショットを送るたびに、次の操作を1つだけ案内してください。
>
> 案内するときは、今開く画面、押すボタン、入力する内容、成功したときの表示を明確にしてください。
>
> パスワード、復旧コード、Secret key、service_roleキー、アクセストークンをチャットへ貼るように指示しないでください。
>
> コードを変更する場合は、変更するファイル、変更理由、影響する機能、元へ戻す方法を先に説明してください。
>
> 既存の回答、修正、集計、ランキング、マッチ、発表モードを壊さないでください。

---

## 導入の順番

次の説明書を番号順に確認してください。

1. `docs/00_最初にお読みください.md`
2. `docs/01_アカウント作成.md`
3. `docs/02_GitHub準備.md`
4. `docs/03_Supabase設定.md`
5. `docs/04_Vercel公開.md`
6. `docs/05_イベント設定.md`
7. `docs/06_QR生成.md`
8. `docs/07_本番前テスト.md`
9. `docs/08_トラブル対応.md`

分からない画面が表示された場合は、先へ進まず、その画面のスクリーンショットをAIへ送ってください。秘密情報が表示されている場合は、該当部分を隠してください。

---

## 最初に作成するアカウント

- GitHub: https://github.com/
- Supabase: https://supabase.com/dashboard
- Vercel: https://vercel.com/dashboard

GitHubアカウントを先に作成すると、SupabaseとVercelでもGitHubアカウントを使ってログインできます。

---

## 自分専用のアプリを作成する

GitHub画面上部の次のボタンを押します。

```text
Use this template
```

続いて、次を選択します。

```text
Create a new repository
```

この操作により、提供元とは別に、自分専用のアプリ保管ページがGitHub上に作成されます。初心者の方は、公開範囲を`Private`にすることを推奨します。

---

## Supabaseにデータの保存場所を作成する

Supabaseで新しいプロジェクトを作成後、GitHubの次のファイルを開きます。

```text
supabase/migrations/202608280001_initial_schema.sql
```

ファイル内のSQLをすべてコピーし、SupabaseのSQL Editorへ貼り付けて実行します。

このSQLにより、回答保存、参加人数保存、最大30名の設定、回答の登録・確認・修正権限、集計者による参加人数変更権限、RLS設定が作成されます。

次の表示が出れば成功です。

```text
Success. No rows returned
```

---

## 集計者のログイン情報を作成する

Supabaseで次の順に進みます。

```text
Authentication
→ Users
→ Add user
→ Create new user
```

集計者用のメールアドレスとパスワードを設定し、`Auto confirm user`をオンにします。メールアドレスとパスワードは安全な場所へ保存してください。

ユーザー作成後、UIDをコピーします。UIDはログインには使いません。集計者だけに回答削除を許可する設定に使います。

GitHubの次のファイルを開きます。

```text
supabase/admin_delete_policy_template.sql
```

`YOUR_ADMIN_UID`を作成した集計者のUIDへ置き換え、SupabaseのSQL Editorで実行します。

---

## Vercelでアプリを公開する

Vercelへログインし、GitHubで作成した自分専用のアプリを選んで取り込みます。

基本設定は次のとおりです。

```text
Framework Preset：Vite
Root Directory：./
```

次の2つの環境変数をProductionへ登録します。

```text
VITE_SUPABASE_URL
VITE_SUPABASE_PUBLISHABLE_KEY
```

### とても重要な注意

2つの値は、必ず同じSupabaseプロジェクトからコピーしてください。

```text
VITE_SUPABASE_URL
→ https://プロジェクトID.supabase.co
```

```text
VITE_SUPABASE_PUBLISHABLE_KEY
→ sb_publishable_で始まるキー
```

Value欄には値だけを入力します。`VITE_SUPABASE_URL=`、引用符、空白、改行は含めません。

Secret key、`sb_secret_`で始まるキー、service_roleキーは使用しないでください。

環境変数を変更した後は、Productionを再デプロイしてください。

---

## 今回の参加人数を設定する

このアプリは最大30名まで対応しています。実際の参加人数は、集計者画面から変更できます。

```text
集計者
→ メールアドレスとパスワードでログイン
→ イベント設定
→ 今回の参加人数を入力
→ 参加人数を保存
```

20名に設定すると、全札数、未回答番号、札番号の入力範囲が1～20へ切り替わり、21番以降は対象外になります。主催者番号も20番になります。

参加人数はSupabaseへ保存されるため、再読み込み後や別端末でも同じ人数が反映されます。

---

## イベント名や商品名を変更する

基本設定は次のファイルにまとめられています。

```text
src/config/eventConfig.js
```

このファイルでは、イベント名、初期参加人数、商品名、ベース量、合計量、選択可能な配合量を変更できます。

変更方法が分からない場合は、ファイルをAIへ確認してもらい、完成版を作成してもらってください。

---

## QRコードを作成する

QR生成用ファイルは次の場所にあります。

```text
scripts/generate-qr.mjs
```

QR検査用ファイルは次の場所にあります。

```text
scripts/verify-qr.mjs
```

QRの枚数は、今回の参加人数と同じ数にしてください。詳しい作成方法は`docs/06_QR生成.md`を確認してください。

---

## 本番前に動作確認する

### 参加人数

- 集計者画面から参加人数を保存できる
- 再読み込み後も参加人数が維持される
- 最後の有効な札番号が使える
- 参加人数を超えた札番号が対象外になる

### 回答

- 初回回答を保存できる
- 再読み込み後も回答済み画面になる
- 回答を修正できる
- 修正しても回答件数が増えない

### 集計

- 回答済み数と未回答番号が正しい
- 人気ベースと人気フレーバーが表示される
- 同じ3種類が正しく集計される
- 配合量まで完全一致した組が正しく集計される

### 発表モード

- 発表モードを開ける
- 前へ、次へ、結果を表示を操作できる
- 人気ランキング、3種類マッチ、完全一致が表示される
- 発表モードを終了できる

### テスト回答の削除

- テスト回答を全削除できる
- 回答済みが0件になる
- 未回答数が参加人数と同じになる

---

## パスワードを忘れた場合

集計者はメールアドレスとパスワードでログインします。UIDではログインできません。

Supabaseで次の順に進みます。

```text
Authentication
→ Users
→ 集計者ユーザー
```

ユーザーを作り直した場合はUIDが変わるため、`supabase/admin_delete_policy_template.sql`のUIDも更新してください。

---

## よくある問題

### アプリが真っ白になる

Vercelの`VITE_SUPABASE_URL`を確認してください。Value欄は`https://プロジェクトID.supabase.co`の形式にし、修正後はProductionを再デプロイします。

### 集計者がログインできない

次を確認してください。

- メールアドレスとパスワード
- Supabaseに集計者ユーザーが存在する
- Auto confirm userを有効にした
- URLとPublishable keyが同じSupabaseプロジェクトのもの
- 環境変数変更後に再デプロイした
- Supabaseプロジェクトが停止していない

### エラーコード42501

Supabaseのデータ操作権限とRLS設定を確認してください。GRANTとRLSは別々の設定です。

### GitHubを変更してもアプリが変わらない

mainへの保存、VercelのReady表示、Productionへのデプロイ、ブラウザーの再読み込みを確認してください。

---

## AIへ画面を送るときの注意

次の情報は隠してください。

- パスワード
- 復旧コード
- Secret key
- service_roleキー
- GitHubやVercelのアクセストークン
- メールアドレス
- 管理者UID
- 個人情報を含む回答内容

Publishable keyも、スクリーンショットでは隠すことを推奨します。

---

## AIに変更してもらった後の確認

1. アプリが正常に表示される
2. 初回回答を保存できる
3. 回答済み画面が表示される
4. 回答を修正できる
5. 修正しても回答件数が増えない
6. 未回答番号が正しい
7. 人気ランキングが正しい
8. 3種類マッチが正しい
9. 完全一致が正しい
10. 発表モードが動く
11. 集計者だけが回答を削除できる
12. 参加人数の設定が維持される

---

## 導入完了の目印

- GitHubに自分専用のアプリ保管ページがある
- Supabaseに回答と参加人数の保存場所がある
- 集計者ユーザーを作成した
- Vercelでアプリが公開されている
- 集計者がログインできる
- 今回の参加人数を設定できる
- 固有QRから回答できる
- 回答を修正できる
- 集計結果を確認できる
- 発表モードを使用できる
- テスト回答を削除できる

ここまで完了したら、QR番号札を印刷し、本番前の動作確認を実施してください。
