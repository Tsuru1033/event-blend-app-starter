# AI作業ルール

## このファイルの目的

このアプリは、AI、GitHub、Supabase、Vercel、プログラミングの初心者が利用します。

AIは、一度に複数の操作を案内せず、利用者が送る画面のスクリーンショットを確認しながら、次の操作を1つずつ案内してください。

操作を案内するときは、次の4点を明確にしてください。

1. 今開く画面
2. 押すボタン
3. 入力する内容
4. 成功したときの表示

---

## 作業前に確認するファイル

変更を始める前に、次のファイルを確認してください。

- `START_HERE.md`
- `README.md`
- `AGENTS.md`
- `AI_HANDOFF.md`
- `AI_PROMPTS.md`
- `src/config/eventConfig.js`
- `supabase/migrations/202608280001_initial_schema.sql`

---

## 最優先ルール

- 既存機能を壊さない
- 変更前に、対象ファイルと変更理由を説明する
- 変更による影響範囲を説明する
- 問題が起きた場合に元へ戻す方法を説明する
- 秘密情報をソースコードへ書かない
- RLSを無効化しない
- `service_role`キーやSecret keyをブラウザーで使用しない
- 1つの札番号につき、回答は1件だけ保存する
- 回答修正では新しい回答を追加せず、既存回答を更新する
- 変更後は既存機能の確認を行う

---

## 秘密情報の取り扱い

次の情報を、AIとのチャット、GitHub、コード、スクリーンショットへ載せないでください。

- パスワード
- 復旧コード
- GitHubアクセストークン
- Vercelアクセストークン
- SupabaseのSecret key
- `service_role`キー
- 管理者UID
- 個人情報を含む回答データ

Publishable keyはブラウザーアプリで使用できますが、スクリーンショットでは隠すことを推奨します。

実際の環境変数はGitHubへ保存せず、VercelのEnvironment Variablesへ登録してください。

---

## 参加人数の仕様

このアプリの最大対応人数は30名です。

実際の参加人数は、Supabaseの次の場所に保存されます。

```text
event_settings
→ participant_count
```

集計者は、アプリの集計者画面から今回の参加人数を1～30名で設定できます。

```text
集計者
→ イベント設定
→ 今回の参加人数
→ 参加人数を保存
```

### 参加人数に関する重要事項

- 最大人数は30名
- 初期参加人数は`src/config/eventConfig.js`で設定する
- 運用中の参加人数はSupabaseの`event_settings`を優先する
- 参加人数を`App.jsx`へ固定値で直接書かない
- 未回答番号は、現在の参加人数を基準に計算する
- 参加者の札番号上限も、現在の参加人数に連動させる
- 参加人数を超えた札番号の回答は、集計対象外にする
- 範囲外の回答を自動削除しない
- 主催者番号は、現在の参加人数と同じ番号へ自動設定する

参加人数を20名に設定した場合は、次の状態を維持してください。

```text
有効な札番号：1～20
札番号20：有効
札番号21：対象外
主催者番号：20
```

---

## イベント設定の変更場所

イベント名、商品名、初期参加人数、配合量などの基本設定は、次のファイルを優先して変更してください。

```text
src/config/eventConfig.js
```

設定値を`App.jsx`の複数箇所へ直接書かないでください。

商品データは、次の形式を維持してください。

```javascript
{ id: 1, name: '商品名' }
```

回答データには商品名ではなく、商品番号を保存する現在の仕様を維持してください。

---

## Supabase接続のルール

Supabaseへの接続には、次の2つの環境変数を使用します。

```text
VITE_SUPABASE_URL
VITE_SUPABASE_PUBLISHABLE_KEY
```

2つの値は、必ず同じSupabaseプロジェクトから取得してください。

```text
VITE_SUPABASE_URL
→ https://プロジェクトID.supabase.co
```

```text
VITE_SUPABASE_PUBLISHABLE_KEY
→ sb_publishable_で始まるキー
```

VercelのValue欄には値だけを入力します。

URLとPublishable keyが別のSupabaseプロジェクトのものになっている場合、ログインや回答保存が正常に動きません。

環境変数を変更した後は、Productionを再デプロイしてください。

---

## 回答保存のルール

回答は、Supabaseの`answers`テーブルへ保存します。

次の仕様を維持してください。

- 札番号は一意にする
- 1つの札番号につき回答は1件
- 初回回答は`INSERT`
- 回答修正は`UPDATE`
- 回答修正後も回答件数を増やさない
- 再読み込み後も回答済み状態を復元する
- ベース、フレーバー1、フレーバー2は異なる商品を選ぶ
- 合計量が設定値と一致することを確認する

---

## 集計機能のルール

次の集計機能を維持してください。

- 全札数
- 回答済み数
- 未回答数
- 未回答札番号
- 人気No.1ベース
- 人気No.1フレーバー
- 同じベースを選んだ人
- 同じ3種類を選んだ人
- 種類と配合量が完全一致した人

人気フレーバーは、フレーバー1とフレーバー2を合算し、選択された回数で集計します。

同率1位の場合は、該当する商品をすべて表示してください。

---

## 発表モードのルール

発表モードでは、次を維持してください。

- 人気No.1ベース
- 人気No.1フレーバー
- 3種類マッチ
- 完全一致
- 前へ
- 次へ
- 結果を表示
- 発表モードを終了

発表モードを開いた時点の集計結果を使用し、発表中に表示内容が突然変わらない仕様を維持してください。

プロジェクター投影を想定し、大きな文字と高いコントラストを維持してください。

---

## QRコードのルール

QR生成ファイルは次の場所にあります。

```text
scripts/generate-qr.mjs
```

QR検査ファイルは次の場所にあります。

```text
scripts/verify-qr.mjs
```

固有QRは次の形式を使用します。

```text
https://公開URL/?ticket=札番号
```

マスターQRは、札番号を含まない公開URLを使用します。

QRを変更した後は、次を実行してください。

```bash
npm run verify:qr
```

次を必ず確認してください。

- 欠番がない
- 重複番号がない
- 印刷番号とQR内の番号が一致する
- 公開URLが正しい
- マスターQRに札番号が含まれていない

---

## 変更後の必須確認

コードを変更した後は、可能であれば次を実行してください。

```bash
npm run build
```

QRを変更した場合は、次も実行してください。

```bash
npm run verify:qr
```

その後、次の機能を確認してください。

1. アプリが正常に表示される
2. 集計者がログインできる
3. 参加人数を保存できる
4. 再読み込み後も参加人数が維持される
5. 最後の有効な札番号が使える
6. 参加人数を超えた札番号が対象外になる
7. 初回回答を保存できる
8. 回答済み状態を復元できる
9. 回答を修正できる
10. 修正しても回答件数が増えない
11. 未回答札番号が正しい
12. 人気ランキングが正しい
13. 3種類マッチが正しい
14. 完全一致が正しい
15. 発表モードが動く
16. 集計者だけが全回答を削除できる

---

## 初心者への案内方法

利用者へコードだけを提示せず、操作方法も説明してください。

GitHub上で編集する場合は、次を明確にしてください。

- 開くファイル名
- 編集ボタンの位置
- 削除する範囲
- 貼り付ける内容
- コミットメッセージ
- 保存後の確認方法

複数箇所を細かく修正するより、初心者が安全に置き換えられる完成版ファイルを作成することを優先してください。

ただし、完成版を生成する前に、現在のファイル内容を確認してください。

---

## エラー対応のルール

エラーが発生した場合は、推測だけで設定を繰り返し変更しないでください。

次の順に原因を切り分けます。

1. エラー全文を確認する
2. 対象がGitHub、Vercel、Supabase、ブラウザーのどれかを確認する
3. Vercelの環境変数を確認する
4. SupabaseのProject URLとPublishable keyが同じプロジェクトか確認する
5. Supabaseのプロジェクトが停止していないか確認する
6. Authログ、Network、Consoleを確認する
7. 原因を特定してから変更する

パスワードを作り直す前に、アプリが正しいSupabaseプロジェクトへ接続していることを確認してください。
